apiVersion: v1
kind: Service
metadata:
  name: sample-app-svc
  namespace: sample-app
spec:
  type: ClusterIP             # internal only
  selector:
    app: sample-app
  ports:
    - port: 80
      targetPort: 80
